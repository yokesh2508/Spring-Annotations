A Spring Core project demonstrating **Prototype Bean Scope** using `@Scope("prototype")`. 
The project shows how Spring creates a new `Student` bean instance each time `getBean()` 
is called, using annotation-based configuration with `@Component`, `@Scope`, `@Configuration`, and `@ComponentScan`.
