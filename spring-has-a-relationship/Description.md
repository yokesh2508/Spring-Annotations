A Spring Core project demonstrating Dependency Injection using Java-based configuration. 
This project showcases `@Component`, `@Autowired`, `@Value`, and `@ComponentScan` 
annotations to implement a HAS-A relationship between `Bank` and `Customer` using the Spring IoC Container.
