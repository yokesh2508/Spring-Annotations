A Spring Core project demonstrating the **Bean Lifecycle** using `@PostConstruct` and `@PreDestroy` annotations. 
The project shows how Spring executes an initialization method after creating the bean and 
a destruction method when the application context is closed.
