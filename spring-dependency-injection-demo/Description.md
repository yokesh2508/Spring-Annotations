A Spring Core project demonstrating the Dependency Injection with Java-based configuration.
This project uses `@Component`, `@Autowired`, `@Value`, `@Configuration`, and `@ComponentScan` 
to inject a `Course` object into a `Student` object through the Spring IoC Container. 
It provides a simple example of annotation-based bean configuration and object dependency management in Spring.
