A Spring Core project demonstrating **Set Collection Injection** using Java-based configuration.
This project showcases how to create and inject multiple `Set<String>` beans using 
`@Bean`, `@Autowired`, and `@Qualifier`. It also demonstrates annotation-based 
configuration with `@Configuration` and `@ComponentScan` by injecting different 
patient collections into a `Hospital` class through the Spring IoC Container.
